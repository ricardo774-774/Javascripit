// Organizar código

// Su Sintaxis era un poco diferente antes,
// ahora ya tenemos modules en JavaScript por lo tanto ya se siente 
// como una solución más natural.

const mostrarCliente = nombre => {
    console.log(nombre);
}

export default mostrarCliente


var module1 = (function() {

})();