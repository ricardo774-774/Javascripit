// Métodos:  .toUpperCase()  .toLowerCase()  .toString()

// .toUpperCase() 
// Te va a permitir cambiar un texto a todo mayusculas
const producto = 'Monitor 24 pulgadas ';
console.log(producto.toUpperCase() );

// .toLowerCase()
// Te va a permitir cambiar un texto a todo minusculas
console.log(producto.toLowerCase() );

// .toString()
// Te va a permitir cambiar un tipo de dato a string
const cantidad = 200;
console.log(cantidad);
console.log(cantidad.toString());